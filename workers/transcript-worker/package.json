{
  "name": "transcript-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@meeting-task/prompts": "workspace:*",
    "@meeting-task/types": "workspace:*"
  }
}
